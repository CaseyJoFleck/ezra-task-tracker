# Local-only Cursor rule (take-home guardrails); do not publish to remote
.cursor/rules/ezra-takehome.mdc

# .NET / SQLite local
**/bin/
**/obj/
**/*.db
**/*.db-shm
**/*.db-wal

# Node / frontend
node_modules/
**/dist/
frontend/dist/

# Local env (never commit secrets; .env.example stays tracked)
.env
.env.local
