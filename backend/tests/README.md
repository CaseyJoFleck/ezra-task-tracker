# Backend tests (placeholder)

Planned test projects (see [docs/implementation-plan.md](../../docs/implementation-plan.md)):

- **Integration tests** — API + SQLite (or test database)
- **Unit tests** — validators, domain rules

No test projects or `.cs` files are committed in this skeleton phase.
